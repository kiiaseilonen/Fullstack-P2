<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Update Song</title>
  </head>
  <body>
    <h1>Update song details</h1>
    <form class="add-form" method="POST" action="/api/update/<%= song._id %>">
      <label for="title">Title:</label>
      <input type="text" name="title" value="<%= song.title %>" /><br />
      <label for="artist">Artist:</label>
      <input type="text" name="artist" value="<%= song.artist %>" /><br />
      <label for="year">Year:</label>
      <input type="number" name="year" value="<%= song.year %>" /><br />
      <label for="imgurl">Img-url:</label>
      <input type="text" name="img_url" value="<%= song.img_url %>" /><br />
      <label for="url">Url:</label>
      <input type="text" name="url" value="<%= song.url %>" /><br />
      <button type="submit">Save</button>
    </form>
  </body>
</html>
